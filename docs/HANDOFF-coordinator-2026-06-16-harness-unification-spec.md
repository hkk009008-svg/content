# Coordinator Handoff - Harness Unification Spec

Generated by coordinator on 2026-06-16 after user requested `handoff`.

## Refresh First

This handoff is a snapshot. Before acting, refresh live state:

```text
env -u GIT_INDEX_FILE .venv/bin/python .agents/skills/four-seat-protocol/scripts/seat_status.py coordinator --wave 2
env -u GIT_INDEX_FILE git log --oneline -8
env -u GIT_INDEX_FILE git status --short
env -u GIT_INDEX_FILE .venv/bin/python scripts/mailbox_monitor.py --once
```

Coordinator is unpinned. Do not consume a coordinator cursor.

## Current Head

- Branch: `main`
- HEAD at handoff write: `2505151a docs(protocol): design harness unification`
- Origin relation at refresh: `1 ahead, 0 behind`
- Working tree before this handoff: clean
- Push status: not pushed by coordinator; push remains user-gated

Recent commits:

```text
2505151a docs(protocol): design harness unification
eff19ee8 codex(protocol): require mailbox-first decisions
5900bcc2 operator2(cursor): consume status replies
74776093 director2(status): reply to coordinator state ping
7caa0460 director(status): reply to coordinator state ping
34e1d1c0 operator2(status): report current no-op state
de673b66 codex(protocol): surface cycle handoff in harness
40e438e7 docs(protocol): require cycle handoff before transplant
```

## What Changed This Session

- User set the strict proof standard for protocol cleanup: keep only parts with
  executable enforcement, repeated incident evidence, or simple
  ambiguity-reducing value.
- User approved the thin-kernel plus checked-adapters design direction.
- Coordinator wrote and committed the design spec:
  `docs/superpowers/specs/2026-06-16-protocol-harness-unification-design.md`.
- Commit: `2505151a docs(protocol): design harness unification`.

No production modules were changed. No inventory transition was made. No
coordinator mailbox event was sent for the design handoff.

## Verification

Commands run before handoff:

```text
env -u GIT_INDEX_FILE .venv/bin/python scripts/wave_gate_check.py 2
```

Result: Wave 2 gate `MET`, `counts={'verified': 30}`, product oracle
`logs/product-oracle-wave2.json`, selector tail `71 passed`.

```text
env -u GIT_INDEX_FILE .venv/bin/python scripts/ci_smoke.py
```

Result: `OK` with the existing advisory/warnings only:

- unknown mailbox kind warning for
  `2026-06-15T19-43-14Z-director2-to-operator2-verify-addendum.md`
- invisible-green warnings for
  `tests/unit/test_discovery_identity_xfail.py:193`
  and `tests/unit/test_lane_silent_gate_siblings_xfail.py:64`

Spec self-review before commit:

- placeholder scan: no unresolved placeholder markers in the spec
- `git diff --check` on the spec: clean
- staged scope before commit: only the new spec file

## Mailbox State

Fresh coordinator/all-scope status showed `ALL-SCOPE EVENTS: 222`; coordinator
has no cursor.

Latest coordinator broadcast:

- `coordination/mailbox/sent/2026-06-16T06-17-17Z-coordinator-to-all-query.md`

Replies read:

- `coordination/mailbox/sent/2026-06-16T06-26-37Z-director2-to-all-status.md`
- `coordination/mailbox/sent/2026-06-16T06-26-38Z-director-to-all-status.md`
- `coordination/mailbox/sent/2026-06-16T06-26-42Z-operator2-to-all-status.md`

Mailbox monitor at `2026-06-16T06:50:06Z`:

```text
receipt split: consumed=3 unread=1 unknown=0
director   unread=3 latest=2026-06-16T06-26-42Z-operator2-to-all-status.md cursor=2026-06-16T06:17:17Z receipt=consumed heartbeat=STALE age=20m
director2  unread=0 latest=- cursor=2026-06-16T06:26:42Z receipt=consumed heartbeat=STALE age=19m
operator   unread=4 latest=2026-06-16T06-26-42Z-operator2-to-all-status.md cursor=2026-06-16T06:06:26Z receipt=unread heartbeat=STALE age=20m
operator2  unread=0 latest=- cursor=2026-06-16T06:26:42Z receipt=consumed heartbeat=ONLINE age=12m
```

Interpretation:

- Director, director2, and operator2 replied with no-op/status evidence.
- Operator has not consumed the `06:17` coordinator ping and has not replied.
- This is a status-cycle gap only; do not invent implementation work from it.

## Locks And Spend

- `coordination/locks/` contains only `.gitkeep`.
- No push, lock claim/release, pod spend, paid API spend, or production edit
  was authorized or performed during this handoff.

## Exact Next Triggers

1. If the next request is to continue the harness-unification work, first read
   `docs/superpowers/specs/2026-06-16-protocol-harness-unification-design.md`,
   then invoke the implementation-planning workflow and write the plan. Do not
   edit runtime protocol surfaces until the plan is approved.
2. If the next request is coordinator status/reconcile, refresh live mailbox
   first. The only known status-cycle gap is operator consuming/replying to
   `2026-06-16T06-17-17Z-coordinator-to-all-query.md`.
3. If the next request is push, fetch first, inspect ahead/behind, and only push
   with explicit user authorization.

## Clean Resume Prompt

```text
continue as coordinator
Read docs/HANDOFF-coordinator-2026-06-16-harness-unification-spec.md first.
Refresh live git/mailbox/gate state before acting. Preserve coordinator
boundaries: no production fixes, no coordinator cursor consumption, no push
without explicit user authorization.
```
