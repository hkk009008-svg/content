# Operator transplant handoff — 2026-06-13 (PM3): PuLID SDXL→FLUX Chunk-1 independently VERIFIED + F1 case-landmine RESOLVED (collision w/ director, reconciled) + ComfyUI restarted + **Task-4 pod acceptance N=1 = GO** (prod PuLID VALIDATED on fp8); N=4 robustness blocked on a ComfyUI cache quirk (one-line fix documented)

**Seat:** operator (= **Pair A operator**, 4-seat protocol LIVE) · **Session:** 2026-06-13 ~09:05Z → ~10:10Z
**HEAD at wrap:** `20a8ca7`. **~15 ahead of origin** (was ~83 earlier — a PUSH appears to have landed mid-session; re-check `git rev-list --count @{u}..HEAD`).
**Gates:** `ci_smoke` **OK** (firsthand). `tests/unit/test_pulid_production_flux.py` **8 passed** (director grew it 5→8). Pod `07ed667` **RUNNING + ComfyUI UP + BILLING** (operator restarted it; census 1106).

## ⭐ #1 PICKUP — the pod is BILLING; remaining burns are DIRECTOR-CLAIMED, operator SCORES
The Pair-A **director claimed the remaining pod burns** in `8c5350b` ("leading N=4 Task-4 robustness + experiment-graft burn", user "use it" spend-go) and asked the operator to **stand down on burns + do independent artifact scoring.** So as the next operator: do NOT burn; **independently SCORE** the director's N=4 + experiment renders as they land (single-artifact, phantom-aware; `scripts/_arc_score_session.py` or the committed `_prod_pulid_acceptance.py` instrument). **⚠️ Pod bills until the user stops it in the Novita console** — surface at pickup.

## ⭐ #2 PICKUP — Task-4 N=4 robustness is blocked on a ComfyUI CACHE QUIRK (one-line fix)
`scripts/_prod_pulid_acceptance.py --n 4` (committed `280fff6`/`20a8ca7`) FAILED at the OFF render: `[n1] completed with NO images`. **Root cause (diagnosed, NOT a real failure):** re-rendering the graph at seed 990011 is byte-identical to the N=1 run → ComfyUI returned `execution_cached` for ALL nodes incl. node 9 SaveImage, `execution_success` in 4 ms, and **EMPTY `/history` outputs** — a full-cache-hit emits no images, so `render_leg` reads "no images" and fails. ComfyUI is healthy (29.4 GiB free). **Fix (pick one):** (a) cache-bust — set a unique `node 9 filename_prefix` per render (e.g. append seed+md5); (b) use FRESH seeds only (990022/33/44/990055 — avoid the N=1-rendered 990011); (c) skip the OFF re-render and reuse the N=1 OFF baseline (0.620) + render only fresh ON seeds. The N=1 GO is unaffected by this.

## ⭐ #3 — Task-4 N=1 acceptance = **GO** (the headline result; prod PuLID fix VALIDATED on the fp8 pod)
Operator ran the A/B itself (user-directed) via committed instrument `scripts/_prod_pulid_acceptance.py` (`a43358f`, `$0 --dry` verified, Rule #22). Same seed 990011 + aria ref + single-subject portrait prompt; OFF = prod `pulid.json` PuLID bypassed (pop 93/97/99/100/101, PAG 301→[112,0]), ON = as-shipped (ApplyPulidFlux, start_at=0.0). Deterministic full-frame arc score (post-`d48b58b`):
- **OFF arc 0.620** [figure, 41% area] — coincidental prompt-match (generic woman, not aria).
- **ON arc 0.878** [figure, 20% area] — aria identity LOCKED. **delta +0.257**; ON ≈ the ~0.87 single-clean-face ceiling (cf man 0.870).
- peak VRAM 18.2 GiB; renders 0.9 / 1.3 min.
**All plan Task-4 Step-4 GO criteria met:** material lift ✓; FaceDetailer-free binding (figure read, no NO_FACE) ✓; **fp8 compat** (node 112 = flux1-dev-fp8 bound to 0.878) ✓; **VISUAL photoreal** (operator viewed both: ON natural skin/no over-cook; OFF a visibly different generic woman) ✓. Artifact `logs/prod_pulid_acceptance_20260613.json` (logs/ gitignored — reproduce via `a43358f`). Verification-report → director `f21d9a4`. **=> the fix is cleared to be the shipping production default (plan Step 5) — the director's flip + ADR-024 close-out (their lane).**

## ⭐ #4 — F1 CASE LANDMINE resolved (collision w/ director; net = clean lowercase rename)
`pulid.json` was git-tracked as capital `Pulid.json` while all 5 code refs + the new test + the on-disk name are lowercase → on a case-sensitive pod checkout the production branch silently FALs (fix unreachable). User authorized the operator `git mv`; operator's `3a74960` botched into a **case-duplicate** (macOS `core.ignorecase=true` silently collapses a case-only *delete*). It COLLIDED with the director's concurrently-claimed comprehensive fix (`d09eef1`). Operator stood down; director's **`a924055`** removed the capital + added `test_production_graph_tracked_lowercase` + the doc-sync (skill docs + PROGRAM-MANUAL). **HEAD now tracks ONLY lowercase `pulid.json`** (blob 83ca7cc, byte-identical a1103bd); case audit zero. Operator F2/F3/F4 advisories all FOLDED by director (`7b54af9`). Lesson logged (sharp edges below).

## Chunk-1 verification verdict (operator, COMPLETE) — CORRECT
`a1103bd` (Pulid.json FLUX swap) / `f05c83b` (workflow_selector start_at=0.0) / `c5199de` (test align) independently VERIFIED: full PuLID family now matches PROVEN `pulid_max.json` node-for-node {97 PulidInsightFaceLoader (correctly unchanged), 99 PulidFluxModelLoader, 100 ApplyPulidFlux, 101 PulidFluxEvaClipLoader}; ApplyPulidFlux key-set identical to max (lldacing fork, pod-valid — census confirmed it loads); wiring 112→100→301(PAG)→17/22→13 genuinely reaches the sampler (not a no-op); start_at→0.0 image-tier only. Report `23dd330`.

## Sharp edges (this session)
1. **ComfyUI execution cache** — re-rendering an IDENTICAL graph+seed = full cache hit → `/history` outputs EMPTY → `render_leg` "completed with NO images" FALSE-failure (NOT an OOM). Cache-bust (unique `filename_prefix`/seed) or reuse the prior artifact. (⭐#2.)
2. **macOS `core.ignorecase=true` silently collapses a case-only rename** → leaves a case-DUPLICATE (both `Pulid.json` + `pulid.json` tracked). The reliable recipe: `git -c core.ignorecase=false …` for the remove/commit (per-invocation, no repo-config change, no peer impact). Verify with `git ls-tree -r HEAD | grep -i pulid` (must be ONE lowercase entry).
3. **The per-seat index-sync hook resets `.git/index-<seat>` to HEAD on commit boundaries** (peers commit every ~30s) → a `git mv` in one Bash call then `git commit` in a LATER call loses the staged rename. Do **rename + commit atomically in ONE Bash call**; pathspec-scoped `commit` can't clobber a peer's parallel commit.
4. **Re-check `git log` + mailbox IMMEDIATELY before committing on a shared task** — my log was minutes-stale and I missed the director's `d09eef1` case-fix claim → collision churn. The director's *test* (`test_production_graph_tracked_lowercase`) was the invariant that made the collision still converge correctly.
5. **`consume-events`/`send-event` are BASH scripts** — don't prefix with `.venv/bin/python`. The coordination linter is `scripts/check_coordination.py`.
6. **Pod billing flag** — bills hourly until the user stops it in the Novita console; pod-work authorization is session-scoped (re-obtain verbatim).

## Cross-seat state at wrap
- **Pair-A director (`director-1`):** ONLINE, claimed the remaining pod burns (`8c5350b`: N=4 + experiment graft). Earlier this session: cleaned the case-landmine (`a924055`), folded F2/F3/F4 (`7b54af9`), routed the determinism siblings continuity_engine:181 + character_manager:396 (`970015b`/`099a1ea`, verification-report `0bf135c`). Pair-A offline backlog CLEARED; remainder pod-gated.
- **Pair B (director2 + operator2):** LIVE on video/assembly-delivery; operator2 doing A/B/C with negative_prompt-forward (`b01fc9f`); 3 trivial fixes STAGED-not-implemented (`5c508d4`). No Pair-A collision.
- **Pod `07ed667`:** RUNNING + ComfyUI UP (operator restart, PID 191) + **BILLING**. Census 1106; FLUX PuLID trio + all identity-critical classes present; 3 char LoRAs intact. Credential/state in `~/.claude/.../memory/pod-ssh-credential.md` (local-only).

*Last verified: 2026-06-13T~10:10Z (ci_smoke OK firsthand; test_pulid_production_flux 8/8 firsthand; HEAD 20a8ca7; Task-4 N=1 GO firsthand — OFF 0.620/ON 0.878 via committed a43358f + visual; N=4 cache-quirk diagnosed via pod /history; pod BILLING — flag to user.)*
