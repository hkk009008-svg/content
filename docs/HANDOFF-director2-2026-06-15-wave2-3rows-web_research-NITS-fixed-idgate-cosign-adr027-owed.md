# Handoff — director2 (Pair-B: video/assembly/audio) — 2026-06-15 (Wave-2 drive session)

**Seat:** director2 (Pair-B director). **HEAD at wrap:** run `git rev-parse HEAD` (peers move it — it moved 5× during this session). Was `8ff2f39`+ my NIT-1 + wrap commits at write time; **ahead of origin ~3** (most of the session got pushed by the coordinator's flow; my tail is local).
**Read this first as the next director2.** Predecessor: `docs/HANDOFF-director2-2026-06-15-task7-verified-wave1-MET-crossthread-filed-nanpoison-carry.md` (now superseded; its CRITICAL carries were drained by peers before this session).
**Campaign:** program-hardening **Wave-2 in progress.** ⚠ NEW doctrine (ADR-027, all seats): **a status-tally "GATE MET" is NOT evidence of correctness** — cite what was mechanically EXECUTED (the operator GO's `--runxfail` RED→GREEN), never the inventory `status` column. Inventory: `docs/REMEDIATION-INVENTORY.md`; S12 triage map: `logs/discovery-wf_f57f0d89-bc2.json` (26 rows → 13 batches + execution order).

---

## TL;DR / session arc
Resumed (user "continue as director2" → "keep driving", ultracode) into an advanced Wave-2. The 2 CRITICAL money rows my predecessor handoff carried were already fixed+confirmed by peers. **Drove 3 Pair-B Wave-2 rows to completion** (lane-only, no lock, no push by me); each pin flipped XPASS→converted to a live regression; each verified in isolation + ci_smoke green; each sent to operator2 for Lane V (impl≠verifier). Mid-session the coordinator (S12) reconciled + re-prioritized (2 PROVISIONAL CRITICALs, ADR-027), director-1 authored the idgate brief + requested my Tier-A co-sign, and operator2 Lane V'd web_research → NITS. Fixed the blocking NIT-1; carried the rest.

---

## What I DELIVERED (3 rows + 1 nit-fix)

1. **`web_research-uncounted` (W2:MAJOR, money) — `f5a95ec` + NIT-1 fix.** Threaded the gate-connected `cost_tracker` through the planning LLM path (`run_with_tools` + `decompose_scene`/`competitive_decompose_scene`/`generate_dialogue`/`generate_style_rules`; `cinema_pipeline.py` passes `self.cost_tracker` at 6 sites; audio-T5 pattern, mirrors `cinema_pipeline.py:552`). Both throwaway `log_llm` → `(cost_tracker or CostTracker())`. **operator2 Lane V (`wf_03568e29-31b`) = NITS** (core fix GO-quality + non-vacuous-mutation-proven). **NIT-1 FIXED** (3rd fallback `scene_decomposer.py:844` was missing the kwarg — my 12-space replace_all missed the 8-space site; operator2 Rule#13 caught it). Scope: pipeline path only; 3 planning HTTP endpoints (`web_server.py` 1438/1479/1520) deferred to the `W2-web_server.py.lock` batch.
2. **`lipsync-syncnet-nan` (W2:MAJOR, silent-gate) — `1d30581`.** `validate_lipsync_quality` (`lip_sync.py:659`) — a NaN/inf SyncNet conf fabricated a perfect 1.0 (`min(1.0, nan)==1.0`). Fix = finite-guard via existing `_finite_or` BEFORE the clamp → 0.0 + structural WARNING. Rule#13: mouth-energy sibling already NaN-safe.
3. **`audio-remux-notimeout` (W2:MAJOR, io/hang) — `f108565`.** `_restore_audio_track` ffmpeg `subprocess.run` had no `timeout=`. Fix = `timeout=30` + added `subprocess.TimeoutExpired` to the except (NOT a CalledProcessError/OSError subclass — without it a hang becomes a crash). Rule#13: only unguarded `subprocess.run` in the file.

All 3 carry committed `verify-request`s to operator2; impl=director2 throughout.

---

## ⭐ OWED BY ME — carries (do in priority order)

1. **🔴 idgate-failopen Tier-A CROSS-LANE co-sign — director-1 is HOLDING DISPATCH on it (now stale → may escalate to user).** director-1 authored the brief (`9fd367d`, `docs/superpowers/briefs/2026-06-15-idgate-failopen.md`) + sent the request (`11ebe90`). It's **PROVISIONAL CRITICAL** (identity-gate fail-open-to-PASS): on prod cloud `DEEPFACE_AVAILABLE=False` → `validate_identity_vision` is the EXCLUSIVE path; its error fallbacks return `default_pass {confidence:0.7}` → `validator.py:1346-1347` `0.7 >= threshold` → forged PASS for every standard tier. **My co-sign (a `verification-report` referencing `idgate-failopen`+the brief, verify at SOURCE not brief-trust) must cover:** (a) ack CRITICAL severity (decisive site `validator.py:1346-1347`); (b) ratify POLICY — director-1 recommends **fail-closed** with evidence (`controller.py:817-847` shows `passed=False` records diagnostics + feeds regen, does NOT deadlock) vs pass-with-warning; (c) confirm the fix covers all error sites (`:261-263` no-key, `:278-280` encode-fail, `:351-353` API-exc) + the Rule#13 sibling decision (`validate_face_quality_vision:244`, `validate_scene_coherence_vision:361` share the pattern — fold or file). Async-OK (workflow+mailbox; no lock — `phase_c_vision.py` non-cross-cutting). operator-1 later confirms the landed diff matches my co-signed scope (drift=FAIL).
2. **🟠 ADR-027 FIX-1/2/4 R-BRIEF — routed to ME as Pair-B director (coordinator `7957c7a`/`19:52` event).** I OWN the brief (campaign-infra; impl≠verifier, a Pair-B operator Lane-Vs each). **FIX-1 (S, highest leverage):** rewrite `scripts/wave_gate_check.py` to **EXECUTE** wave-tagged pins (`pytest <wave pins> --runxfail -q`) + derive MET from exit-0/XPASS-clean; `status` column becomes display-only. ⚠ **WILL flip Wave-1 "MET 8/8" → UNMET for test-infeasible/non-XPASS-clean rows — that flip is the intended, user-ratified outcome.** **FIX-4 (M):** new `scripts/pin_reconciler.py` — re-run `verified` rows' pins WITHOUT `--runxfail`, flag still-XFAIL. **FIX-2 (S, after FIX-1):** wire `wave_gate_check.py` into CI on push to main. Honor the critic flags: FIX-7 deferred (ARC CI-vacuity); FIX-1 does NOT close 2 gaps (operator-GO impl≠verifier not gate-enforced; unregistered defects escape). Test-infeasible rows are now **`attested`** not `verified`.
3. **🟠 NEW ROW: `LLMEnsemble.competitive_generate` cost-leak (candidate-CRITICAL, operator2-filed).** `llm/ensemble.py` has ZERO cost tracking; the DEFAULT planning path (`competitive_generation=True`, `cinema_pipeline.py:1017`) makes 3 LLM calls/scene (2 candidates + judge) invisible to the gate, scaling with scene count. PRE-EXISTING (separate module; my f5a95ec didn't create it). **BLOCKS web_research reconcile-to-verified** (would mask the default-path leak — I agreed with operator2). Needs: coordinator R-VERIFY-TIER severity triage (money-gate=CRITICAL vs lane-v=MAJOR split) + a strict-xfail pin + fix. Cleanest fix = **option (b): post-call `log_llm` on the result `usage`** (already carried at `_generate_anthropic:285`) — no interface change. NOT yet filed as a numbered inventory row — coordinator to file.

---

## Owed BY others / state
- **operator2:** Lane V pending on `lipsync-syncnet-nan` (`1d30581`) + `audio-remux-notimeout` (`f108565`); re-verify the web_research NIT-1 diff. (Verify-requests all committed to mailbox.)
- **coordinator:** triage the LLMEnsemble row severity + file it; reconcile syncnet/audio-remux on operator GO; web_research stays **fixed (not verified)** until LLMEnsemble closes.
- **director-1:** WRAPPED (`8ff2f39`); A1 lora-decouple landed `23c99e3` (operator-1 Lane V owed); idgate dispatch HELD on my co-sign.
- **Push:** user/coordinator-gated; I pushed nothing unilaterally (the coordinator's flow pushed most of the session).

## NEXT director2 (in order)
(1) Rule #8 surface mailbox; (2) **idgate Tier-A co-sign FIRST** (director-1 blocked + stale); (3) ADR-027 FIX-1 brief (highest-leverage infra); (4) brief/triage the LLMEnsemble row; (5) then resume draining no-lock Pair-B MAJORs (`cost-conn-crossthread-drop`, `audioflag-inherit`, `download-urllib-notimeout`); (6) `charmgr-cost-fresh-instance` is now PROVISIONAL CRITICAL — its complete fix touches `web_server.py:587` (lock+push, Tier-A). Consult the S12 triage `logs/discovery-wf_f57f0d89-bc2.json` for batch order; don't improvise.

## Sharp edges (this session)
- **`replace_all` indentation trap:** an indented `old_string` only matches that indent level. My 12-space `return decompose_scene(...)` pattern silently skipped the 8-space outer-except sibling (`:844`) → an incomplete fix the cold reviewers missed; operator2's Rule#13 param-completeness scope-match caught it. When threading state through recursive/cascade sites, grep ALL sites + diff each vs its siblings for forwarded state. ([[operator2_rule13_param_completeness_on_recursive_edits]])
- **Dirty shared tree = no full-suite attribution.** A peer's uncommitted A1-lora-decouple WIP (quality_max.py) lived in the worktree; pytest counts were contaminated. Verified each fix via its ISOLATED test file + ci_smoke; committed explicit-pathspec only. HEAD moved 5× under me (`dd6377a`→…→`8ff2f39`); Rule#7 before every commit.
- **A line-adding fix shifts ARCHITECTURE.md anchors (hard gate).** web_research +lines moved `competitive_decompose_scene` 624→626 + `_assemble_final` 1323→1324 → ci_smoke RED until I fixed the 2 same-line anchors (NOT a blanket `--fix` — that sweeps 100+ advisory PROGRAM-MANUAL anchors). Lane owners fix-on-touch (coordinator S12 §7).
- **A well-shaped pin tests the SEAM, not the production wiring.** Both web_research + charmgr pins pass once the leaf accepts `cost_tracker=`; production-completeness (threading the live tracker down) is separate — do BOTH or the money hole stays open. (operator2's non-vacuity caveat: dropping a pipeline caller's kwarg leaves the pin GREEN.)
- **Finding:** `lipsync-postproc-costkey` pin is MIS-SHAPED (tests `record_api_call("syncsov3")` directly; the correct call-site fix won't flip it) — routed to coordinator for pin rework.
