---
cell-id: B-tier
tier: B
executor: operator-seat
project-id: 7cddd0c59f6d
project-name: Tier B Korean Probe 2026-05-27
wall-clock-min: ~17 (2 attempts: ~5 first frozen + ~5 reset + ~7 second run)
cost-actual-usd: 0.58 (tracked) + ~$0.7-1.2 (untracked LLM/BGM/Foley/TTS) ≈ $1.30-1.80 cumulative
success-flag: PASS-WITH-2-CRITICAL-1-IMPORTANT-2-MINOR
related-commits: 2426f59, 972e239, 3a4abb8
related-rules: 2, 4, 5, 7, 8, 9, 15
status: COMPLETE
---

# Tier B single-shot probe — Korean dialogue variant

User-§9 Q6 minimal probe variant with Korean dialogue to exercise Cartesia Sonic 2 language router (per `cac8650` + `9ba2eb8`).

**Project:** `7cddd0c59f6d` — *Tier B Korean Probe 2026-05-27*

**Composition:**
- 1 scene: "혼자만의 시간 (A Moment Alone)" — contemplative mood, Seoul cafe
- 1 character: "민지 (Min-ji)" — Korean, late 20s, no PuLID ref (scope narrowing per dispatch-claim `2426f59`)
- 1 location: "서울 카페 (Seoul Cafe)" — warm afternoon, Hongdae
- 1 shot: medium close-up, zoom_in_slow, cinematic_glow
- 1 dialogue line (Korean): "오늘은 정말 길었어... 그래도 다시 시작할 수 있을 거야."
- `global_settings.budget_limit_usd: 50.0` (I-A6.1 Option a applied)
- `global_settings.language_pref: "ko"` (canonical field is actually `language`; see I-B1 below)

**Two pipeline runs:**
- **Run 1 (`b2a75hwzy`):** Started T19:34Z; froze at KEYFRAME_REVIEW gate (image_veto_on_fallback default True vetoed FAL fallback keyframe). Killed at T19:43Z. Findings: C-B1 + I-B1 surfaced; pipeline correctness validated through P-STYLE / P-BGM / PR-DIALOGUE / P-KEYFRAME.
- **Run 2 (`bih8ftetb`):** Reset shot state + `auto_approve.image_veto_on_fallback: False` + I-B1 partial fix shipped (`972e239`). Started T19:47Z; completed pipeline end-to-end through P-MOTION + P-FOLEY + P-ASSEMBLY at T19:52:08Z. Stuck at post-assembly SCREENING gate (env-var-driven; project-level setting ineffective — see M-B1). Killed at T19:54Z. `final_cinema.mp4` written successfully at T19:52:08Z.

---

## Per-cell observations

### P-STYLE ✅ PASS

**PREDICTION:** Style rules JSON generated. ~2-4 LLM rounds. Time ~30-60s. Cost ~$0.10-0.30.

**ACTUAL (Run 1):**
- Generated successfully at T04:35:59 (~30s)
- 4 OpenAI chat completion calls
- 2 [WEB] search rounds (cinematography research)
- "Research-enhanced with cinematography reference (3 sources)"
- Cached in `project.json.global_settings.style_rules` — Run 2 skipped regeneration (idempotent ✅)

**DELTA:** PASS-with-INSIGHT.

**INSIGHT:** Style rules generator does real web search (Tavily) for cinematography references. 4 LLM calls = 1 initial + 2 search rounds + 1 final synthesis. This is the load-bearing path for getting cinematography references — NOT a pure LLM call. Time + cost dominated by network research, not LLM tokens.

**ADJUSTMENT:** None — feature works as architected.

### P-BGM ✅ PASS

**PREDICTION:** FAL Stable Audio kick-off. Research-enhanced. ~30-60s gen time. Cost ~$0.10-0.50.

**ACTUAL (Run 1):**
- FAL Stable Audio Stable Audio 2.0 queue completion ~3 min wall-clock (including queue + 36s actual inference per direct status probe)
- Music vibe `contemplative` resolved via vibe_prompts dict (line 286 in audio/music.py: "Cinematic ambient music, contemplative mood, slow, atmospheric, film score quality") — vibe NOT explicitly in vibe_prompts dict; fell through to default cinematic ambient template
- Output: `temp/bgm_contemplative.mp3` (8.3 MB)
- Pedalboard mastering: `temp/bgm_contemplative_mastered.mp3` (1.9 MB; `cinema_master` preset, 4 effects)
- Run 2 skipped regeneration since files cached (idempotent ✅)

**DELTA:** PASS-with-INSIGHT.

**INSIGHT (I-B2 MINOR):** "contemplative" mood is NOT in the curated `vibe_prompts` dict at `audio/music.py:249-289`. The dict has 30 moods (suspense / thriller / horror / noir / dystopian / melancholic / romantic / bittersweet / grief / hopeful / epic / action / triumphant / chase / ethereal / dreamy / meditative / cosmic / cyberpunk / corporate / gritty / urban / uplifting / jazz_noir / classical / western / electronic_minimal). Falls through to generic template. **For brief consistency, "contemplative" should either be added to the dict OR remapped to an existing close mood (e.g., "melancholic" or "meditative").** Not blocking — generic template produced acceptable BGM — but worth a cycle-16+ vibe-coverage extension.

**ADJUSTMENT:** Recommend Lane D candidate: add "contemplative" to `vibe_prompts` in `audio/music.py` OR document the fall-through as intentional in audio architecture section of ARCHITECTURE.md §12.

### P-DECOMPOSE ⚠️ DEFERRED (scope narrowing)

**PREDICTION:** Skipped per dispatch-claim `2426f59` — manual shot precludes auto-decompose (`cinema_pipeline.py:890` skips when `scene.shots` present).

**ACTUAL:** Confirmed skipped — pipeline used my pre-defined shot directly.

**DELTA:** DEFERRED. Test in Tier C with multi-shot scene OR isolated decompose test in cycle-16+ (~$0.20-0.50 LLM cost).

### P-CHIEFDIR ⚠️ NOT EXERCISED (scope narrowing path)

**PREDICTION:** ChiefDirector reviews shot prompts. Possible outcomes: APPROVED / MODIFIED / BLOCKED. Cost ~$0.05-0.20.

**ACTUAL:** The pipeline path at `cinema_pipeline.py:905-916` only invokes ChiefDirector AFTER decompose (`if not shots: ... competitive_decompose_scene ... director.validate_shot_prompts`). Since decompose was skipped (manual shot), ChiefDirector validation was also skipped. **Tier C with auto-decomposed shots WILL exercise P-CHIEFDIR.**

**DELTA:** NOT_EXERCISED (scope narrowing).

**INSIGHT:** P-CHIEFDIR's invocation is coupled to P-DECOMPOSE in the orchestration; cannot independently test ChiefDirector validation in single-shot manual-plan path.

### PR-DIALOGUE / Cartesia routing 🟡 PARTIAL — primary target NOT exercised (I-B1)

**PREDICTION:** Per `cac8650` _resolve_tts_provider, `language_pref="ko"` + Korean text + CARTESIA_API_KEY set should route to Cartesia (CARTESIA_SONIC_2 model). Cost via cost_tracker `CARTESIA_SONIC_2` entry per `fb25677` I-2 fold. ~$0.02-0.10 for ~5s of Korean speech. Time ~2-5s.

**ACTUAL (both runs):**
- Dialogue WAS generated (~$0.02-0.05) — `temp_dialogue_line_0.mp3` + `audio_scene_*.mp3`
- BUT routed to **ElevenLabs** (`api.elevenlabs.io/v1/text-to-speech/pNInz6obpgDQGcFmaJgB`), NOT Cartesia
- Pipeline log: NO `[CARTESIA]` marker; voice ID `pNInz6obpgDQGcFmaJgB` = ElevenLabs Adam (default voice when character.voice_id is empty)

**DELTA:** FALSIFIED (I-B1 IMPORTANT). Primary target of Korean probe variant NOT exercised.

**INSIGHT (I-B1):** Two-layer routing problem:
1. **Resolver-side**: `_resolve_tts_provider` (audio/dialogue.py:122) reads `scene["language"]` / `character["language"]` / default `"en"` — does NOT consult `settings_obj.language_pref`. **Fixed at `972e239` (added settings_obj.language_pref fallback consultation).**
2. **Dispatcher-side**: `audio/dialogue.py:334` constructs synthetic `scene_for_router = {"language": get_project_setting(ctx, "language", "English")}` — reads key `"language"`, NOT `"language_pref"`. **Caller bypasses my resolver fix by passing pre-resolved scene.** The CANONICAL field per codebase grep is `global_settings["language"]` (used in `web_server.py:409,1331`, `cinema_pipeline.py:946`, `llm/prompt_optimizer.py:418`, `audio/dialogue.py:334`). `language_pref` is a brief-doc alias that has NO actual binding in code.

**Resolution paths:**
- **(a) Per-project workaround:** Set `global_settings["language"] = "Korean"` in the project — matches code canonical. Forward Tier C path.
- **(b) Brief-level fix:** Update brief docs to use canonical `language` field consistently (drop `language_pref` alias).
- **(c) Dispatcher fix:** Patch `audio/dialogue.py:334` to read both `language` and `language_pref` keys for backward compat with any future brief usage. ~3 LoC.

**ADJUSTMENT (recommended):** Combine (a) + (c) — set `language: "Korean"` in Tier C project + ship dispatcher fix for resilience. Cycle-16+ follow-up cell can validate Cartesia routing end-to-end with proper field naming.

### P-KEYFRAME 🟡 PASS-WITH-FALLBACK (C-B1)

**PREDICTION (no-PuLID variant calibration per director Flag #2):** FLUX-fp8 generation on RunPod pod via PuLID-FLUX workflow. Identity-related sub-criteria marked N/A. Non-identity sub-criteria: latency 15-45s, cost ~$0 (pod amortized), image 1024×576 (16:9), file written to disk.

**ACTUAL:**
- Local ComfyUI rejected workflow: `node_errors.{112}.UNETLoader.value_not_in_list: "unet_name: 'FLUX1/flux1-dev-fp8.safetensors' not in []"`
- Pipeline gracefully fell back to FAL FLUX-Pro (line: `[FALLBACK] FLUX-Pro (no face-lock)`)
- FAL FLUX-Pro completed in ~10-15s (after queue)
- Image saved: 770 KB, `domain/projects/7cddd0c59f6d/shots/shot_90e3dee8da44/outputs/take_e65b9accd9ee.jpg`
- Cost (tracked): $0.04 per FAL FLUX_KONTEXT call ($0.08 total across both runs)
- Run 2 auto-approved after fallback (`auto_approve: gate='image' shot='shot_90e3dee8da44' — approved`) since `image_veto_on_fallback: False` was set

**DELTA:** PASS-WITH-CRITICAL-FALLBACK (C-B1).

**INSIGHT (C-B1):** A9 pre-flight (T19:22Z) probed `CheckpointLoaderSimple.input.required.ckpt_name` and confirmed `FLUX1/flux1-dev-fp8.safetensors` present. But the keyframe workflow uses **`UNETLoader`** (different node, different model list — `models/diffusion_models/` or `models/unet/` dirs). UNETLoader's list is EMPTY on the pod. The FLUX model file is in `models/checkpoints/` (as a CheckpointLoaderSimple option), not in the UNet path the workflow needs.

**This is a load-bearing Tier C blocker.** PuLID-FLUX identity consistency tests REQUIRE local pod → UNETLoader → FLUX-fp8 path. Without it, every keyframe falls back to FAL FLUX-Pro WITHOUT identity anchoring. Cross-shot identity drift in Tier C reel becomes unmeasurable.

**Fix scope (pod-side, NOT this branch):** Update `scripts/setup_runpod.sh` to symlink OR copy `FLUX1/flux1-dev-fp8.safetensors` from `models/checkpoints/` to `models/diffusion_models/` (preferred path; ComfyUI 0.22.0 default). Add candidate #5 to cycle-15 close handoff's setup_runpod.sh hardening list (already lists 6 candidates; this is #7).

**ADJUSTMENT (Tier C unblock):** Pod-side fix required BEFORE Tier C. Operator should either:
- SSH to RunPod pod and create the symlink manually:
  ```
  ln -sf /workspace/ComfyUI/models/checkpoints/FLUX1/flux1-dev-fp8.safetensors \
        /workspace/ComfyUI/models/diffusion_models/flux1-dev-fp8.safetensors
  ```
- OR update `scripts/setup_runpod.sh` + run from local
- OR change keyframe workflow to use CheckpointLoaderSimple instead of UNETLoader (likely larger code change)

### P-MOTION ✅ PASS (Kling Native)

**PREDICTION:** Veo or fallback engine. 5s @ 24fps. Time ~30-90s. Cost ~$0.50-1.00.

**ACTUAL:**
- Routed to **Kling Native** (`pro` model, 5s)
- Queue + processing: 193s total (cycle-15 v0.9.3 PA-VIDEO Set 3 KLING_NATIVE per `f413aa3` brief alignment)
- Downloaded: 10.2 MB → `take_9111b4f1f68a.mp4`
- Cost (tracked): $0.50

**DELTA:** PASS (within prediction tolerance + on expected engine per v0.9.3 brief).

**INSIGHT:** Kling Native completion time (193s) is at the upper end of 30-90s prediction. Operator suggestion: brief PA-MOTION cell PREDICTION should extend latency band to 60-240s for KLING_NATIVE specifically (Veo path is faster but PA-VIDEO Set 3 is now KLING_NATIVE-default per v0.9.3).

### P-FOLEY ✅ PASS (Stability Stable Audio)

**PREDICTION:** Stability AI Stable Audio 2.0 (cycle-15 v0.9.6 feature). 5s ambient/foley clip. Cost ~$0.10-0.30. Time ~10-30s.

**ACTUAL:**
- Generated successfully at ~T04:52:00 (~5-10s wall-clock)
- Foley prompt: "subtle ambient room tone..."
- Output: `temp/foley_scene_327c8437d6d9.mp3`
- Cost (NOT tracked in `cost_log`): est $0.10-0.30

**DELTA:** PASS.

**INSIGHT:** Stability Stable Audio 2.0 integration from cycle-15 entry works first-attempt. Foley prompt "subtle ambient room tone" is the scene's `scene_foley` field or a generated default for the cafe location.

### P-ASSEMBLY 🔴 CRITICAL FAILURE — tri-mix path broken (C-B2)

**PREDICTION:** FFmpeg stitch + color correction + tri-mix (voice + BGM + foley) + loudnorm two-pass. Time ~10-30s. Cost $0 (local FFmpeg).

**ACTUAL:**
- Stitch ✅ (single-shot, trivial; `temp/stitched.mp4`)
- Color grade ✅ (`warm_cinema` preset; `temp/graded.mp4`)
- **Tri-mix FAILED ❌**:
  ```
  WARNING: BGM mix failed, trying BGM-only fallback
  stderr_tail: [1:a]volume=0.12[bgm];[2:a]volume=0.20[foley];
               [voice][bgm][foley]amix=inputs=3:duration=first:dropout_transition=2[aout]
               matches no streams.
               Error binding filtergraph inputs/outputs: Invalid argument
  ```
- Fell back: "Final cinema video assembled (BGM only, no dialogue audio)"
- Loudnorm 2-pass ✅ (-34.70 LUFS → -14.0 LUFS target)
- Final output: `final_cinema.mp4` (2.0 MB, h264 video + 1 aac audio stream = BGM only)

**DELTA:** FALSIFIED (C-B2 CRITICAL).

**INSIGHT (C-B2):** Tri-mix filtergraph `[voice][bgm][foley]amix=inputs=3` couldn't bind `[voice]` and `[foley]` streams. The dispatcher provided BGM as input `[1:a]` and foley as input `[2:a]` (both mapped) but the `[voice]` label has no input mapping declared in the visible filtergraph snippet. This is a **stream-binding bug** in the FFmpeg amix call construction.

Likely cause: `phase_c_ffmpeg.py` (per ARCHITECTURE §12 / brief v0.9.6 audio-architecture-extension) builds the tri-mix filtergraph but the `[voice]` input is conditional on dialogue file existing — if the file path is wrong / missing / not passed as input -i flag, the label remains unbound. Cycle-15 v0.9.6 brief states foley is "additive to existing voice+BGM mix" but if voice mapping itself is broken at this assembly, foley extension fails downstream.

**The final video has BGM only** — Korean dialogue line (regardless of ElevenLabs vs Cartesia source) is MISSING from the assembled cinema MP4. Foley is also likely dropped (only "BGM only" mentioned in fallback log; no separate foley-only fallback observed).

**Fix scope:** Investigation needed at `phase_c_ffmpeg.py` tri-mix construction. Suspected: missing `-i {voice_audio_path}` flag mapping when constructing the FFmpeg command, OR an off-by-one in input index labels. Both runs hit identical error — this is reproducible.

**ADJUSTMENT (recommended):** Spawn a `fix(audio):` task investigating `phase_c_ffmpeg.py` tri-mix filtergraph stream binding. Cycle-16+ priority CRITICAL because:
- Tri-mix is the brief's load-bearing cycle-15 audio architecture feature (v0.9.6 + v0.9.7)
- Every final video produced this branch silently drops dialogue
- Falsifies foley + Cartesia work without obvious user-visible error

### Post-assembly SCREENING gate behavior (M-B1)

**OBSERVATION:** After `final_cinema.mp4` was written at T19:52:08, pipeline entered SCREENING gate and polled `is_screening_approved(project)` indefinitely. My project-level `global_settings.screening_stage_enabled: False` was IGNORED — `_screening_stage_enabled()` at `cinema/screening.py:84` reads ONLY env var `CINEMA_SCREENING_STAGE`, not project setting. Default: enabled.

**DELTA:** M-B1 MINOR (env-vs-project setting precedence mismatch).

**INSIGHT (M-B1):** The brief v0.9.X+ documentation suggests project-level settings control screening stage; actual implementation is env-var-driven. Either:
- **(a)** Patch `_screening_stage_enabled()` to consult project `global_settings.screening_stage_enabled` as fallback for env-var-not-set case
- **(b)** Document env-var-only behavior in ARCHITECTURE.md §7.7.3
- **(c)** NO ACTION — env var works fine for headless/CI runs; UI runs always have operator approval available

Pipeline workaround for Tier B: killed pipeline at T19:54 after `final_cinema.mp4` confirmed on disk; no actual SCREENING approval needed since artifact-of-interest already exists.

---

## Cost summary

**Tracked in cost_log (data/experiments.db):**
| Operation | Provider | Model | Cost USD | Notes |
|---|---|---|---|---|
| keyframe_generation | fal | FLUX_KONTEXT | $0.04 | Run 1 fallback |
| keyframe_generation | fal | FLUX_KONTEXT | $0.04 | Run 2 fallback |
| motion_generation | kling | KLING_NATIVE | $0.50 | Run 2 (Run 1 didn't reach motion) |
| **Subtotal tracked** | | | **$0.58** | |

**NOT tracked (estimated, gap M-B2):**
| Component | Provider | Est USD | Notes |
|---|---|---|---|
| Style rules LLM (research + synthesis) | OpenAI GPT-4o | ~$0.05-0.15 | 4 calls per run × 2 runs = 8 calls |
| BGM | FAL Stable Audio | ~$0.20-0.40 | 1 generation (cached for Run 2) |
| Dialogue TTS | ElevenLabs | ~$0.02-0.05 | 1 line, ~5s, 2 runs |
| Foley | Stability AI | ~$0.10-0.30 | 1 generation (Run 2 only) |
| Prompt optimizer LLM | OpenAI/Anthropic | ~$0.05-0.10 | Per shot |
| ChiefDirector validation | LLM | $0.00 | Skipped (decompose-skip path) |
| Scene decomposition LLM | LLM | $0.00 | Skipped (manual shot) |
| **Subtotal untracked** | | **~$0.42-1.00** | |

**Cumulative Tier B cost estimate: ~$1.00-1.58** (well under Q6 $3-7 envelope; cumulative project under $50 hard cap).

**M-B2 (MINOR) finding:** `cost_log` SQLite table records only keyframe + motion calls. LLM + BGM + Foley + TTS NOT tracked. `cost_tracker.py` has API_COST_USD dict for `STABILITY_FOLEY` + `CARTESIA_SONIC_2` per brief v0.9.6/v0.9.7/v0.9.8 cycle-15 entry, but the recording call sites are not consistently invoked across all providers. Tracking gap should be closed before Tier C (where budget enforcement at $50 ceiling matters more).

---

## Findings catalog

| ID | Severity | Description | Disposition |
|---|---|---|---|
| **C-B1** | 🔴 CRITICAL | ComfyUI UNETLoader empty list → all keyframes fall to FAL FLUX-Pro (no PuLID identity anchoring) | Tier C blocker; pod-side fix via setup_runpod.sh symlink OR workflow change |
| **C-B2** | 🔴 CRITICAL | Tri-mix audio assembly `[voice][bgm][foley]amix` fails stream binding; final video has BGM only (no dialogue, likely no foley) | Cycle-16+ priority `fix(audio):` at phase_c_ffmpeg.py |
| **I-B1** | 🟡 IMPORTANT | Cartesia Korean routing NOT triggered. Brief's `language_pref` field is unbound; canonical is `language`. Resolver patched (972e239) but dispatcher reads wrong key. | Combined fix: dispatcher fallback to both keys + Tier C uses `language: "Korean"`. |
| **I-B2** | 🟡 MINOR | "contemplative" mood not in `vibe_prompts` dict; falls through to generic | Lane D candidate: add to dict OR document fallthrough |
| **M-B1** | 🟡 MINOR | SCREENING gate ignores project-level `screening_stage_enabled`; env-var-only control | Document OR add project-setting fallback |
| **M-B2** | 🟡 MINOR | cost_log incomplete: only keyframe + motion tracked; LLM/BGM/Foley/TTS not recorded | Cycle-16+ tracking-completeness sweep before Tier C |

---

## Tier C unblock status

| Gate | Status | Notes |
|---|---|---|
| Tier B pipeline executes end-to-end | ✅ PASS (Run 2) | `final_cinema.mp4` written; 2.0 MB; h264 video + aac audio |
| Tier B core flows validated | ⚠️ PARTIAL | STYLE / BGM / FAL-keyframe / Kling-motion / Foley work; dialogue path wrong route + tri-mix broken |
| **C-B1 pod-side fix** | 🔴 BLOCKED | PuLID-FLUX path requires UNETLoader list populated; pod-side action needed before Tier C identity tests |
| **C-B2 tri-mix fix** | 🔴 BLOCKED | Audio architecture cycle-15 feature broken; every Tier C reel will have BGM-only audio without fix |
| I-B1 fix | 🟡 RECOMMENDED | Either set `language: "Korean"` in Tier C project OR ship dispatcher fix (~3 LoC) |
| M-B1 / M-B2 | 🟡 OPTIONAL | Operational improvements; not Tier C blockers |

**Tier C SHOULD NOT proceed until C-B1 and C-B2 are addressed.** Tier C goal is identity-consistent multi-shot reel; without PuLID-local + tri-mix, the reel can't be fairly assessed.

---

## Director Flag #2 calibration (no-PuLID variant)

Per director's `T19:34:00Z` ack Flag #2: identity-related sub-criteria for P-KEYFRAME marked **"N/A (Tier B no-PuLID variant)"** rather than "FALSIFIED" — preserves falsifiability discipline. Applied throughout P-KEYFRAME section above.

---

## Race-ack telemetry summary (cycle-16 entry, post-Tier-B-end)

| # | Shape | Instance | Cycle |
|---|---|---|---|
| 1 | Concurrent-claim race | T19:19:51Z + T19:19:53Z dispatch-claims | 16 entry |
| 2 | Stale-mailbox-content assertion | My `2426f59` §"Coordination" #1 stale by ~2.5 min | 16 entry |
| 3 | Pre-write re-verify gap (Flag #1 director observation) | My T19:31:45Z write skipped `git log -3` precondition | 16 entry |

All N=1 of distinct shapes; none at N=2 emergence. Recurrence in Tier C dispatch will trigger Candidate #8 N=2 codification proposal.

---

Signed,
Operator-seat — 2026-05-27 cycle 16 entry, Tier B Korean dialogue probe COMPLETE (2 attempts; 2 CRITICAL + 1 IMPORTANT + 3 MINOR findings; `final_cinema.mp4` produced; Tier C blocked on C-B1 + C-B2)
