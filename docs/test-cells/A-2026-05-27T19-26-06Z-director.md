# Tier A pre-flight — director cold-context parallel observation

**Date:** 2026-05-27 (cycle-16 entry)
**Session start (director):** ~T19:12Z
**Tier A run window (director):** T19:22Z–T19:26Z (~4 min wall-clock)
**Author:** director-seat (cold-context independent observation)
**HEAD at run start:** `e523a4e` (director race-ack)
**Baseline commit (post-pod-restart + A6 fix):** `0ecda24`
**Tier-end verdict (director side):** ✅ **ALL A1-A9 GREEN** with 2 MINOR brief-doc-drift findings (Lane D candidates at tier close)

This artifact is the director-side cold-context independent observation per Rule #9 §"Parallelism" + operator's `3cc7a84` §"Coordination with director" #2 explicit welcome. **Operator-seat's primary tier artifact lives at `5425e5e`** — director did NOT read operator's artifact before composing this; convergence step happens at tier-close cross-compare.

---

## Per-cell observations

### A1 — Working tree state + baseline

**PREDICTION (brief §3 line 205):** `main...origin/main, clean`

**ACTUAL:**
```
$ git status -sb
## main...origin/main [ahead 1]

$ git log --oneline -5
e523a4e coord(mailbox): director race-ack on operator's Tier A dispatch-claim 3cc7a84 + parallel cold-context observation
3cc7a84 coord(mailbox): operator Tier A dispatch-claim — user-principal "begin all testing" authorization received
f0e5c0c coord(mailbox): operator cycle-16 entry ack — cursor T10:46:03Z → T19:13:28Z + standby
5fd2e58 coord(mailbox): director cycle-16 entry ack — cursor T08:35Z → T10:56:16Z + receipt of 0ecda24 A6 closure
0ecda24 fix(settings): use override=True on load_dotenv so .env wins over shell environment
```

**DELTA:** ✅ **PASS.** Branch tracking line matches; WT clean. "ahead 1" is director's own `e523a4e` race-ack — not yet pushed to origin per default risky-action protocol. Not a fault.

---

### A2 — Static gates

**PREDICTION (brief §3 lines 208-211):**
- `scripts/ci_smoke.py` → OK
- `(cd web && npx tsc --noEmit)` → exit 0
- `(cd web && npm run build)` → clean build

**ACTUAL:**
```
$ .venv/bin/python scripts/ci_smoke.py
OK

$ cd web && npx tsc --noEmit
(silent; exit 0)

$ cd web && npm run build
> cinema-production-tool@1.0.0 build
> tsc && vite build

vite v6.4.1 building for production...
transforming...
✓ 85 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                   0.62 kB │ gzip:   0.42 kB
dist/assets/index-CPA0a1mc.css   44.17 kB │ gzip:   9.03 kB
dist/assets/index-BFGdbBgc.js   457.82 kB │ gzip: 121.24 kB
✓ built in 740ms
build exit=0
```

**DELTA:** ✅ **PASS.** All three gates clean. 85 modules; 740ms build; bundle sizes nominal (JS 457KB / gzip 121KB; CSS 44KB / gzip 9KB).

---

### A3 — Full unit suite

**PREDICTION (brief §3 lines 214-216):** `866 passed, 3 skipped, 0 failed (cycle-14 baseline post dbcde8b). Cycle-10 "flake" RETIRED at dbcde8b — should NO LONGER appear as failure.`

**ACTUAL:**
```
$ .venv/bin/python -m pytest tests/unit/ --tb=no -q | tail -1
925 passed, 3 skipped, 2 warnings, 10 subtests passed in 28.88s
```

The 2 warnings are deliberate emissions from `tests/unit/test_cost_tracker.py::TestLogLLMUnknownModel` — the tests INTENTIONALLY trigger the `UserWarning: [cost_tracker] Unknown model ...` via `cost_tracker.log_llm(model='llama-3-70b'|'mistral-large')` to verify warning emission. Not regressions; expected behavior under test.

**DELTA:** 🟡 **MINOR — brief baseline stale.** Brief expects 866; actual 925 (+59). Audio additions cycle-15 entry (foley `c15b2a8` + Cartesia `d4f11d6`/`cac8650`/`9ba2eb8`) added new test files (`test_audio_dialogue_cartesia.py` 27 tests + foley/checkpoint test additions). Brief §3 line 215 needs update from 866 → 925.

**Lane D candidate at tier-close:** brief §3 line 215 baseline refresh.

---

### A4 — ARCHITECTURE.md §15 smoke

**PREDICTION (brief §3 line 219):** Same as A2; doc-of-truth alias.

**ACTUAL:** ≡ A2 (single source: `.venv/bin/python scripts/ci_smoke.py` → OK).

**DELTA:** ✅ **PASS.**

---

### A5 — RunPod ComfyUI pod alive

**PREDICTION (brief §3 lines 221-224):** `HTTP/2 200 OK or HTTP/1.1 200 OK`

**ACTUAL:**
```
$ curl -sI "$COMFYUI_SERVER_URL/object_info" --max-time 15 | head -3
HTTP/2 200
date: Wed, 27 May 2026 19:24:25 GMT
content-type: application/json; charset=utf-8
```

URL resolved from `.env`: `https://525nb9d5cc0p3y-8188.proxy.runpod.net`.

**DELTA:** ✅ **PASS.** HTTP/2 200 + JSON content-type. Pod is alive + responding on object_info endpoint.

---

### A6 — LLM provider keys + budget headroom

**PREDICTION (brief §3 lines 226-230):** OPENAI/ANTHROPIC/FAL keys set; `CINEMA_BUDGET_LIMIT_USD` env var documented for review.

**ACTUAL:**
```
$ for k in OPENAI_API_KEY ANTHROPIC_API_KEY FAL_KEY CARTESIA_API_KEY SUNO_API_KEY GEMINI_API_KEY ELEVENLABS_API_KEY STABILITY_API_KEY; do ...; done
OPENAI_API_KEY: set (len=164)
ANTHROPIC_API_KEY: set (len=108)
FAL_KEY: set (len=69)
CARTESIA_API_KEY: set (len=29)
SUNO_API_KEY: set (len=32)
GEMINI_API_KEY: set (len=39)
ELEVENLABS_API_KEY: set (len=51)
STABILITY_API_KEY: set (len=51)

$ .venv/bin/python -c "from config.settings import settings; print(...)"
anthropic_api_key len: 108        ← matches `0ecda24` post-fix diagnostic
openai_api_key len: 164
fal_key len: 69
cartesia_api_key len: 29
suno_api_key len: 32
gemini_api_key len: 39
elevenlabs_api_key len: 51
stability_api_key len: 51

$ .venv/bin/python -c "from config.settings import settings; print(getattr(settings, 'cinema_budget_limit_usd', 'N/A'))"
cinema_budget_limit_usd: N/A      ← settings attribute doesn't exist
```

**Budget mechanism re-verified:** budget enforcement is **per-project setting** (`project["settings"]["budget_limit_usd"]`), NOT a global env var. Sourced at `cinema/core.py:99` `budget_usd = settings.get("budget_limit_usd")` (where `settings` is the per-project settings dict), constructed into `CostTracker(budget_usd=budget_usd)`. `CINEMA_BUDGET_LIMIT_USD` env var **does not exist** in `.env` or `config/settings.py`.

**DELTA:** 🟡 **MINOR — brief comment stale.** All 8 API keys present + load via `config.settings` (post-`0ecda24` `override=True` fix confirmed: ANTHROPIC len=108). Brief §3 line 230 comment "review .env CINEMA_BUDGET_LIMIT_USD" is incorrect — no such env var exists. Actual budget mechanism is per-project setting at project creation time (read in `cinema/core.py:99`).

**Lane D candidate at tier-close:** brief §3 line 230 either (a) update comment to "Per-project: `project.settings.budget_limit_usd` set at project creation; $50 hard cap per user-§9 Q-6", OR (b) remove the comment if redundant with §9.

---

### A7 — Identity validator weights

**PREDICTION (brief §3 lines 232-242):** `ghostfacenet_v1.h5` ~16-25MB present; DeepFace.build_model('GhostFaceNet') loads OK.

**ACTUAL:**
```
$ ls -la ~/.deepface/weights/ghostfacenet_v1.h5
-rw-r--r--@ 1 hyungkoookkim staff 17271840 Mar 27 21:03 /Users/hyungkoookkim/.deepface/weights/ghostfacenet_v1.h5

$ .venv/bin/python -c "from deepface import DeepFace; DeepFace.build_model('GhostFaceNet'); print('GhostFaceNet model loaded OK')"
GhostFaceNet model loaded OK
```

File size: 17,271,840 bytes = ~16.5MB (within brief's 16-25MB range). File mtime: 2026-03-27 (cached from prior cycle).

**DELTA:** ✅ **PASS.** Weights present + cached; DeepFace model factory builds without network call (no auto-download warmup needed at execution).

---

### A8 — Sample project ready

**PREDICTION (brief §3 lines 244-254):** baseline count recorded; if reuseable populated project exists, prefer reuse; else fresh minimal creation.

**ACTUAL:**
```
$ ls domain/projects/ | wc -l
       0

$ ls -la domain/projects/
total 0
drwxr-xr-x@  3 hyungkoookkim staff   96 May 27 03:16 .
drwxr-xr-x@ 16 hyungkoookkim staff  512 May 27 01:02 ..
-rw-r--r--@  1 hyungkoookkim staff   0 May 23 02:35 .gitkeep
```

Baseline project count: **0** (only `.gitkeep`).

**DELTA:** ✅ **PASS** with explicit Q6 alignment. Empty state aligns with user-§9 Q6 answer "FRESH MINIMAL ~$3-7 cost". No reusable populated project; Tier B will create fresh project via UI per Q6.

**Tier-B prep note:** when Tier B begins, fresh project creation flow needs:
- ≥2 scenes (per Q6 sub-check)
- ≥1 character
- ≥1 dialogue
- ≥1 location

Pytest-leakage baseline = 0 records to /tmp/projects-baseline.txt; post-Tier-execution delta target = (fresh-project-created count). If pytest fixtures leak (delta > expected), surface at tier-close.

---

### A9 — ComfyUI workflow probe

**PREDICTION (brief §3 lines 256-273):**
- A9.1: 9 expected nodes (`PulidFluxInsightFaceLoader`, `PulidFluxModelLoader`, `ApplyPulidFlux`, `PulidFluxEvaClipLoader`, `ControlNetApply`, `IPAdapter`, `VAELoader`, `CheckpointLoaderSimple`, `FluxGuidance`) all present in `/object_info`
- A9.2: FLUX-1-dev or similar checkpoint loaded

**ACTUAL:**
```
$ curl -sf "$COMFYUI_URL/object_info" --max-time 30 | jq -r 'keys[]' > /tmp/comfyui-nodes.txt
Total nodes: 867

A9.1 node coverage:
OK: PulidFluxInsightFaceLoader
OK: PulidFluxModelLoader
OK: ApplyPulidFlux
OK: PulidFluxEvaClipLoader
OK: ControlNetApply
OK: IPAdapter
OK: VAELoader
OK: CheckpointLoaderSimple
OK: FluxGuidance

A9.2 checkpoint inventory:
FLUX1/flux1-dev-fp8.safetensors
```

**DELTA:** ✅ **PASS.** All 9 expected nodes present (9/9). FLUX1-dev-fp8 checkpoint loaded — matches brief v1.0 SHIP entry "pod 525nb9d5cc0p3y A5/A9 GREEN" + `0f6527f` body's pod inventory.

---

## Summary

| Cell | Predict | Actual | Delta | Notes |
|---|---|---|---|---|
| A1 | main…origin/main clean | main…origin/main [ahead 1] clean | ✅ PASS | "ahead 1" = director's own race-ack; not yet pushed |
| A2 | OK + tsc 0 + clean build | OK + tsc 0 + 740ms build | ✅ PASS | 85 modules; JS 457KB / CSS 44KB |
| A3 | 866 pass / 3 skip / 0 fail | 925 pass / 3 skip / 0 fail | 🟡 MINOR | brief §3:215 baseline stale — +59 tests vs cycle-14 baseline (foley + Cartesia additions); Lane D candidate |
| A4 | OK | OK (≡A2) | ✅ PASS | doc-alias |
| A5 | HTTP 200 | HTTP/2 200 | ✅ PASS | JSON content-type |
| A6 | keys set | 8/8 keys set + loaded (ANTHROPIC len=108) | 🟡 MINOR | brief §3:230 comment stale — `CINEMA_BUDGET_LIMIT_USD` env var doesn't exist; actual = per-project setting; Lane D candidate |
| A7 | h5 ~16-25MB + builds | 17.3MB + builds OK | ✅ PASS | cached weights; no warmup needed |
| A8 | baseline count recorded | baseline = 0 (only .gitkeep) | ✅ PASS | aligns with Q6 FRESH MINIMAL; Tier B creates fresh |
| A9 | 9 nodes + FLUX checkpoint | 9/9 nodes + FLUX1-dev-fp8 | ✅ PASS | 867 total nodes available |

**Tier A pre-flight all-green criterion (brief §3:276):** A1-A9 all return expected values. ✅ **MET.** Brief execution NOT paused.

---

## Findings catalog (director cold-context, this artifact)

| F# | Severity | Cell | Type | Brief location | Recommendation |
|---|---|---|---|---|---|
| **D-A1** | MINOR | A3 | DOC-DRIFT | brief §3:215 | Update baseline `866 passed` → `925 passed` to reflect cycle-15-entry foley + Cartesia test additions. Add comment noting incremental count from cycle-14 baseline 866 + 27 (Cartesia) + 32 (foley + checkpoint + dispatch). Lane D candidate. |
| **D-A2** | MINOR | A6 | DOC-DRIFT | brief §3:230 | Either (a) update comment to "Per-project: `project.settings.budget_limit_usd` set at project creation; $50 hard cap per user-§9 Q-6"; or (b) remove if redundant with §9. `CINEMA_BUDGET_LIMIT_USD` env var doesn't exist. Lane D candidate. |

No CRITICAL or IMPORTANT findings on director side. Tier B is unblocked from director's cold-context observation.

---

## Convergence step (post-commit)

This artifact is committed independently; cross-compare with operator's `5425e5e` artifact happens AFTER this commit, via `verification-report` event back to operator. Per Rule #9 cold-context independence, the comparison is meaningful only if both artifacts were produced without reading the other.

**Director-side commit subject form:** `test(cell): A-tier <verdict> (director cold-context parallel) — Tier A pre-flight A1-A9 ($0)`

---

## Tier B GO recommendation (director side, pre-convergence)

✅ **GO** from director's cold-context observation. All A1-A9 green; 2 brief-doc-drift MINOR findings are Lane D candidates, not Tier B blockers.

Tier B execution next-actions:
1. Cross-compare with operator's `5425e5e` artifact (verification-report event)
2. Surface to user-principal for Tier B GO authorization (per brief §1 + Q6 fresh-minimal project creation flow)
3. Pre-Tier-B: create fresh minimal project via UI per Q6 (~$3-7 cost; ≥2 scenes / ≥1 character / ≥1 dialogue / ≥1 location)

---

**Signed:** Director-seat — 2026-05-27 cycle 16 entry, Tier A pre-flight cold-context parallel observation complete. All A1-A9 GREEN. 2 MINOR Lane D candidates (brief §3:215 baseline + §3:230 budget comment). Tier B GO from director side, pending operator convergence + user-principal authorization.
