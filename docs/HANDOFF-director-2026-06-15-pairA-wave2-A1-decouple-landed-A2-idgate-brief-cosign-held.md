# HANDOFF — Director (Pair-A), 2026-06-15 — Wave-2: A1 lora-decouple LANDED + dual-reviewed; A2 idgate CRITICAL brief authored, co-sign HELD

**READ FIRST AS PAIR-A DIRECTOR.** This was a Wave-2 hardening session (NOT the realism/pod lane — pod is STOPPED). Two Pair-A rows advanced: **A1 closed in code; A2 (idgate) brief authored + Tier-A co-sign requested but NOT yet landed.** Trust `git log -1`, not this prose.

## State at wrap (trust git)
- HEAD **`7957c7a`**, **12 ahead of origin** (push user-gated; I pushed nothing). My commits this session: `23c99e3` (A1 fix), `df005b1` (A1 coord), `9fd367d` (A2 brief), `11ebe90` (A2 coord). The 3 commits after mine (`271944a`/`ededed1`/`7957c7a`) are coordinator ADR-027 meta-work.
- **All peers STALE at wrap** (director2 1h18m, operator 2h02m, operator2 1h09m). Pod **STOPPED** (user did it; $0).
- ci_smoke OK at my last check; `git diff HEAD` shows the usual **phantom-index** noise (≈50 peer files as D/?? that are on-disk+in-HEAD — NOT real; the tree is intact). Every commit this session used **explicit pathspec** + verified `git show --diff-filter=D` empty.

## ⚠ ADR-027 doctrine adopted mid-session (coordinator `ededed1`, user-ratified) — APPLIES TO YOU
**A status-tally "GATE MET" is NOT evidence of correctness.** Cite what was mechanically EXECUTED (an operator GO's `--runxfail` RED→GREEN), never the inventory `status` column, as a correctness claim. (This is why A1 below is `fixed`, NOT `verified` — see A1.) Also: **FIX-5** — from Wave-2 on, a wave does not close without ≥1 committed **product-oracle artifact in `logs/`** (Pair-A identity rows ⇒ an ArcFace-arc measurement, R-MEASURE). **FIX-1** (Pair-B-director task) will rewrite `wave_gate_check.py` to EXECUTE pins → may re-grade gates. Test-infeasible rows are now `attested`, not `verified`.

## A1 — `has-char-lora-hole` (MAJOR) + `secondary-lora-hole` (MEDIUM): CODE LANDED `23c99e3`
Root: the single `has_character` flag (`quality_max.py:1060`) keyed off the primary face-ref file only but gated the whole identity stack → a **LoRA-only shot silently rendered with zero identity** (LoRA node 700 pruned + `_inject_identity` early-return); secondary-LoRA injection shared the root.
- **Fix (brief `docs/superpowers/briefs/2026-06-15-has-character-decouple.md`):** decoupled `has_face_ref` (PuLID/face/ArcFace) from `has_char_lora` (node 700 + secondary gate). **Keystone:** a 700-aware FLUX-incompat bridge (`_surviving_model_src`, priority 100>700>112) keeps a LoRA-only shot's LoRA REACHABLE from BasicGuider(22) — `22.model→[700,0]`, NOT bypassed to base `[112,0]`. A pin-only `"700" in wf` check would have shipped an **orphaned (silently bypassed) LoRA**; the new reachability test is the guard.
- **Reviewed (orchestrated, impl≠me-verifying):** graph-reachability GO (4 shot cases, 0 dangling) + brief-conformance GO. Both xfail pins flipped to live regressions; full suite 2497p/0f; ci_smoke OK.
- **Real catch:** conformance reviewer found my brief's grep MISSED 16 experiment scripts calling `_prune_unavailable` positionally — I grepped the FLAG name, not the FUNCTION name. Fixed behavior-preservingly (`X→X,X`); AST-verified 0 stale call sites repo-wide.
- **STATUS = `fixed`, NOT `verified` (ADR-027):** the correctness claim awaits **operator-1's EXECUTED Lane V** (verify-request sent `18:44Z`; KEY ask = node-700 REACHABILITY, not presence). Inventory rows still read `open` at HEAD — I backed my edit out (coordinator online owns the doc); coordinator notified to reconcile `open→fixed`, then `verified` on operator-1 GO. **FIX-5 caveat:** closing these on the gate will want a `logs/` ArcFace-arc oracle artifact.

## A2 — `idgate-failopen` (PROVISIONAL CRITICAL): brief authored, co-sign REQUESTED, **DISPATCH HELD**
Brief `docs/superpowers/briefs/2026-06-15-idgate-failopen.md` (committed `9fd367d`). **CRITICAL CONFIRMED first-hand** (the coordinator's S12 upgrade is right; the `phase_c_vision.py:338-341` "never governs a real gate" comment is wrong for the error path):
- prod cloud `DEEPFACE_AVAILABLE=False` (`identity/validator.py:399-403`) → `validate_identity_vision` is the EXCLUSIVE identity gate. Its 3 error fallbacks (no-key `phase_c_vision.py:261-263`, encode-fail `:278-280`, API-exc `:351-353`) return `confidence:0.7`; validator `:1346-1347` does `matched = 0.7 >= threshold` → forged PASS for every standard tier (only strict-portrait .75 escapes). `source="default"` discarded → unobservable.
- **Recommended POLICY = fail-closed** (verified SAFE: `cinema/shots/controller.py:817-847` treats a failed gate as advisory+remediation, no deadlock; persistent outage → best-of-N ships best take WITH failure recorded, never a forged pass). Mechanism: error fallbacks return a NON-PASS marker mirroring the validator's existing `skip`/`missing_generated` mapping; bounded retry before fail-closed; no-key fails closed immediately + loud WARNING.
- **HELD (§6c/Rule #23):** director2 Tier-A `verification-report` owed BEFORE I dispatch (request sent `18:59Z`, `11ebe90`, references defect-id + brief). **director2 WRAPPED (`0de878a`) and CARRIED the co-sign as their explicit #1 next-session task** — they did NOT ghost it; their handoff (`docs/HANDOFF-director2-2026-06-15-wave2-3rows-web_research-NITS-fixed-idgate-cosign-adr027-owed.md`) restates all 3 scope points correctly and queues "idgate Tier-A co-sign FIRST". → next director-1: the co-sign is QUEUED, not lost. Check the mailbox for a director2 `verification-report` referencing `idgate-failopen`; **on GO → dispatch the fix → operator-1 Lane V. If it stays unanswered while director2 is stale, ping then escalate to the user-principal** (silence ≠ consent; never self-dispatch a CRITICAL cross-lane fix).
- **operator-1 owes** the R-VERIFY-TIER ratification of the MAJOR→CRITICAL upgrade (evidence broadcast `18:59Z`; can fold into the idgate-fix Lane V).
- **Open Rule #13 question for director2:** fold the 2 sibling vision gates (`validate_face_quality_vision`, `validate_scene_coherence_vision` — same fail-open pattern) into this fix, or file as separate rows? (The scene-coherence sibling relates to the coordinator's unregistered `coherence-silent` caller-side half.)

## OWED by the next Pair-A director (priority order)
1. **A2 idgate co-sign:** check mailbox for a director2 `verification-report` referencing `idgate-failopen` + brief. **If landed (GO):** dispatch ONE implementer per the brief (the fail-closed mechanism) → operator-1 Lane V. **If still pending + director2 stale:** escalate to user (do not self-dispatch). On a fail-open→fail-closed POLICY change, author a one-line DECISIONS.md ADR on GO.
2. **Remaining Pair-A Wave-2 rows (lane-only, no co-sign):** `identity-nan-arc-bypass` (`face_validator_gate.py:326`, MEDIUM — NaN arc bypass, zero isfinite guards in the file) and `coherence-silent` (`coherence_analyzer.py:202`, MEDIUM). **Coordinator flagged** the `coherence-silent` **caller-side MAJOR half** (`controller.py:2266`, `.valid` never checked → color_grade suppression) has **NO row + NO pin** — an ADR-027 "unregistered defect"; expand scope or file a new row.
3. Read the **stub-contract spec** `docs/superpowers/specs/2026-06-15-wave2-stub-contract.md` before authoring any W2 test (idgate = a gate-fail row, NOT coverage-only).

## Sharp edges (this session)
- **Phantom-index pollution** (Workflow runs leave skip-worktree D/?? on recent files): `git diff HEAD`/`git status` LIE. Guard = explicit-pathspec on `git commit -- <files>` (commits working-tree of only those paths; phantom deletions structurally cannot enter) + verify `git show --diff-filter=D --name-only HEAD` empty. Do NOT `git read-tree`/`git add -A` (peers were live earlier).
- **zsh ≠ bash word-split:** the Bash tool runs zsh; `git commit -- $FILES` passed the whole list as ONE pathspec (zsh doesn't split unquoted `$var`). Fix = list files inline (or `${=FILES}`).
- **Grep the FUNCTION name, not the flag name,** for call-site completeness — grepping `has_character` missed 16 scripts calling `_prune_unavailable` positionally; a conformance reviewer caught it. AST arg-count check is the definitive re-verify.
- **Distrust reassuring comments.** `phase_c_vision.py:338-341` claimed the 0.7 "never governs a real gate" — true for the success path, FALSE for the error path. A comment about a call graph can silently rot when config flips (`DEEPFACE_AVAILABLE`).
- **Inventory dual-write avoidance:** coordinator was ONLINE editing the inventory (S12) → I did NOT write it (backed my edit out; notified the coordinator to reconcile). §6f: coordinator is primary inventory writer while online.
- **Tree moved +6 under me; Rule #7 caught it** before each commit (peer money/lipsync/coord lanes — no quality_max collision).

## Cursor / mailbox
Read through **`2026-06-14T19:52:48Z`** (coordinator ADR-027 broadcast). Sent: A1 verify-request→operator-1, A1 coord→all, A2 co-sign→director2, A2 ratification→all. Owed-to-me: nothing landed. [[program_hardening_campaign_live]] [[has_character_lora_only_hole]] [[money_loss_gate_source_mismatch_bug_class]]
