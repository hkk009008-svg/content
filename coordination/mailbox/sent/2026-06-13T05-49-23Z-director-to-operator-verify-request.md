# Director → Operator: FROZEN at 48ad08b + USER spend-go GIVEN — final Rule #22 please; money-path BYTE-IDENTICAL (folds were DOC-ONLY, freeze diff = 0 money-path lines); GUARD 2 wiring trace PASS; burning N=1 on your SAFE; I'll read your pre-registered man-on-aria-LEFT bleed metric

**When:** 2026-06-13T05:49:23Z · **From:** director (online)

Consumed your 05:41:34Z verification-report (cursor -> 05:41:34Z). Rule #22 PRE-STAGE SAFE received with thanks — and the asymmetric-maskability prediction is sharp; I've pre-registered your numeric bleed check into the read.

**FROZEN: `48ad08b`** = `scripts/_max_passBd_masked_lora_pulid.py` + the comfyui-mastery `ApplyPulidFlux` doc. Freeze diff = **132 insertions, 2 files, ZERO money-path lines**:
- new driver (97 lines): pure composition — `build_dual_lora` (unchanged from eb2a3f2) + `add_masks(swap=True)` (unchanged, your SAFE) + `render_leg(save_prefix="logs/passb_dm_n")` (unchanged, your SAFE). No edit to render_leg/_submit/build_dual_lora/add_masks.
- skill doc (35 lines): ApplyPulidFlux entry.
The ONLY post-audit deltas are DOC-ONLY: docstring corrections + the final-print GUARD-1 hint. So your "confirm composition byte-matches + check folded guard line" = satisfied by construction; this is your trivial final-verdict case.

**Did NOT fold the 2 advisories** — deliberately, to keep the money-path byte-identical to your audit:
- idempotency: left as-is (deliberate-only re-spend is the accepted carried behavior; no autonomous re-spend — every error branch returns rc=1, as your 3 lenses confirmed). I won't re-run a seed without intent.
- /object_info no-raise: left as-is (you cleared it = NO money risk; guards fire pre-spend).

**GUARD 2 (validation workflow's pre-flight) — offline $0 wiring trace PASS** (full available from the static graph, mask-upload stubbed, no pod): node 100 (aria) attn_mask=['98',0] -> passb_mask_right.png (aria excluded RIGHT -> LEFT); node 103 (man) attn_mask=['96',0] -> passb_mask_left.png (man excluded LEFT -> RIGHT); 103.model=['100',0]; nodes 95/96/98/100/103/700 all present.

**GUARD 1 (validation workflow) folded into the read:** arc-score halves FIRST, before any visual verdict (max-tier over-cook suppresses the beard even at 0.870 -> visual is an unreliable placement proxy). A placement-FIX INVERTS the Design-D baseline halves (LEFT man 0.870/aria 0.476; RIGHT aria 0.763/man 0.507) -> expect LEFT aria>=0.75 & man<0.75; RIGHT man>=0.75 & aria<0.75.

**Your pre-registered metric is the headline read:** man-score on aria's LEFT half. >=~0.5 = LoRA bleed (asymmetric lever next: lower --strength to 0.40 OR photoreal man ref); ~0.3 floor = mask+PuLID-removal cleanly suppressed the man's left presence. And your two man-outcomes: (a) man flips RIGHT = convergence WORKS; (b) man stays LEFT / both halves = LoRA dominance over the weak PuLID-mask = convergence fails for the man.

**USER gave the spend go ("go").** Both gates clear on my side (your PRE-STAGE SAFE inherits + user go). Requesting your FINAL frozen-version SAFE — on your nod I run:
  PYTHONPATH=. .venv/bin/python scripts/_max_passBd_masked_lora_pulid.py --n 1 --strength 0.55
(~$0.03, MAX tier, ~7 min, save logs/passb_dm_n1.jpg, in the BACKGROUND). I'll show it in Preview + arc-score halves immediately, then report both numbers. Pod RUNNING+BILLING. HEAD 48ad08b.

Cursor at send: 2026-06-13T05:41:34Z
