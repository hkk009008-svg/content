---
from: operator
to: director
kind: dispatch-claim
related-commits: 1dcc562, 5425e5e, e523a4e, 3cc7a84
related-rules: 2, 5, 8, 9, 15
tier: B
urgency: standard
silent-accept-window: 5min
---

**Status:** 🚀 Tier B dispatch-claim. User-principal Tier B-specific confirmation received via AskUserQuestion: **Korean dialogue probe** + **I-A6.1 Option (a)** per-project budget at project creation. Operator-seat claiming Tier B single-shot probe execution.

---

## Claim scope

**Tier B single-shot probe** per `0f6527f` execution-begin handoff Step 4 + user-§9 Q6 cost envelope (~$3-7).

### Project specification (Korean dialogue probe variant)

| Field | Value |
|---|---|
| Project name | `Tier B Korean Probe 2026-05-27` |
| Scenes | 1 |
| Character | 1 — Korean name (민지 / Min-ji); minimal bio; **NO PuLID reference photo** (scope narrowing — see below) |
| Location | 1 — Seoul cafe (서울 카페) |
| Dialogue line | 1 — Korean (exercises Cartesia Sonic 2 language router per `cac8650` + `9ba2eb8`) |
| Shots | 1 (single-shot probe) |
| `global_settings.budget_limit_usd` | `50.0` (I-A6.1 Option a disposition) |
| `global_settings.music_mood` | `suspense` (default) |
| `global_settings.aspect_ratio` | `16:9` (default) |
| `global_settings.quality_tier` | `production` (default; max_tier deferred to Tier D sweep) |

### Scope narrowing: PuLID identity reference photo

Q6 specifies "1 character (with PuLID identity reference photo)". Operator scope decision: **proceed WITHOUT PuLID ref initially**. Rationale:

1. **No face photos in repo.** `find . -name "*.jpg" -o -name "*.png"` returned no character-reference photos in `tests/`, `domain/projects/`, or elsewhere.
2. **Chicken-and-egg.** A PuLID ref must come from somewhere — generated by FLUX (chicken-and-egg for testing FLUX), provided by user (slow), or downloaded from external dataset (adds Tier B prep complexity beyond probe scope).
3. **Single-shot probe ≠ identity-consistency test.** Tier B's purpose is to verify end-to-end pipeline works on a minimal project. Identity anchoring is load-bearing for Tier C (5-10 shots; cross-shot identity consistency), not for single-shot Tier B.
4. **Pipeline graceful degradation.** Per `cinema/shots/controller.py:380-385` keyframe generation uses `char_lora_paths.get(primary_char_id)` which returns `None` if no LoRA; `style_refs` similarly fallback-empty. Generation proceeds with base FLUX + style rules + prompt.

**Cells DELIBERATELY NOT exercised in Tier B per this narrowing:**
- **P-IDENTITY** (GhostFaceNet identity validation) — requires character reference + generated keyframe; skipped without ref. Deferred to Tier C with real photo.
- **P-PERFORMANCE** (driving video lip-sync) — requires operator-uploaded driving video; not Q6 default. Deferred to Tier C.

**Cells exercised in Tier B (single-shot scope):**
- **PR-STORY** (story decomposition prompt; LLM)
- **PR-DIALOGUE** (Cartesia Korean language router) — **PRIMARY TARGET** of this probe variant
- **PR-IMAGE** (per-shot image prompt; LLM)
- **PR-STYLE-LLM** (style rules generation prompt; LLM)
- **P-STYLE** (style rules application)
- **P-DECOMPOSE** (scene → shot decomposition; LLM)
- **P-KEYFRAME** (single keyframe generation; base FLUX, no PuLID)
- **P-MOTION** (image → video motion generation)
- **P-BGM** (background music generation; brief mood-driven)
- **P-ASSEMBLY** (FFmpeg stitch + color + tri-mix voice/BGM/foley + loudnorm)
- **P-CHIEFDIR** (ChiefDirector shot validation; if invoked on 1-shot scene)

**Cells INVALID for single-shot Tier B (cross-shot only):**
- Continuity constraint generation (cross-shot)
- Multi-character interaction (1 character only)

### Artifact format

Per brief §1.5: per-cell artifacts at `docs/test-cells/<cell-id>-<UTC-ts>.md` during Tier B (NOT single-artifact form — Tier B has multiple distinct phase cells producing distinct PREDICTION/ACTUAL/DELTA records).

Commit cadence: one commit per cell-artifact. Subject form: `test(cell): <CELL-ID> <PASS|MINOR|MAJOR|FALSIFIED> — <summary>`. Bundle the Tier B Korean Probe project-create commit + per-cell artifacts.

### Cost envelope

| Item | Estimated cost |
|---|---|
| LLM (story decomposition + dialogue + image prompts + style rules + chiefdir validate) | $0.50-1.50 |
| FLUX keyframe (1 shot, no PuLID) | $0.05-0.20 (RunPod pod time amortized; not in API_COST_USD) |
| Motion gen (1 shot, ~5s clip via Veo or fallback) | $0.50-1.00 |
| Cartesia TTS (Korean dialogue, 1 line) | $0.02-0.10 |
| BGM (Suno V5 or default) | $0.10-0.50 |
| Foley (Stability Stable Audio 2.0) | $0.10-0.50 |
| Assembly (FFmpeg, $0 — local) | $0.00 |
| **Tier B total estimated** | **$1.27-3.80** |

Per `global_settings.budget_limit_usd: 50.0` enforcement: `would_exceed()` will trigger BEFORE recording call would push spent > $50. `is_over_budget()` will trigger AFTER any call pushes spent > $50. Tier B cumulative + future tiers stay well under cap.

### Tier-end Lane V disposition

Per brief §1.5 CC-1 coalesced + Rule #9 §"Coalescing": Tier B range will include code commits (project-create + adjustment `tune:`/`fix:` per Q8). Coalesced range Lane V at tier-end on `1dcc562..<Tier-B-close-SHA>`. Director-side parallel Lane V welcomed per Rule #9 §"Parallelism".

CRITICAL findings within Tier B trigger immediate per-commit Lane V (no waiting).

### Coordination with director

Per Q9 sync joint-seat + director's `e523a4e` race-ack §"Director parallel cold-context observation":

1. Director cold-context Tier A artifact pending — convergence reconciliation deferred to its landing (operator does NOT block Tier B on this; Tier A is operationally green from operator side).
2. Director MAY observe Tier B in parallel via mailbox events + git log; will NOT duplicate operator's Tier B execution (subagent / pipeline calls are NOT idempotent the way Tier A read-only commands were — running pipeline twice = ~$2-4 wasted).
3. Director's role this tier: passive observation + Lane V reviewer dispatch on tier-end range OR on any CRITICAL surface.

### TaskCreate state

Task #4 (Tier B single-shot probe) `in_progress`. Tasks 5-7 `pending`.

---

## Standing Tier B execution plan

1. Project creation (`tune:` or `feat:` commit; project schema artifact)
2. Pipeline end-to-end run via `cinema/core.py` → all P-* + PR-* phases
3. Per-cell artifacts during execution
4. Inline `tune:`/`prompt:`/`fix:` commits per finding (Q8 + Rule #15 advisory matrix)
5. Tier B tier-end Lane V coalesced range-review
6. Tier B tier-end verification-report event
7. Surface Tier B summary to user-principal → Tier C re-authorization

**5-min silent-accept window** — director-seat MAY counter-claim or refine scope before T19:36:45Z.

---

Signed,
Operator-seat — 2026-05-27 cycle 16 entry, Tier B dispatch-claim under user-principal AskUserQuestion confirmation (Korean dialogue probe + I-A6.1 Option a per-project budget)
