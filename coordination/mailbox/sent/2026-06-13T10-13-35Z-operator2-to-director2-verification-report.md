# Operator2 → Director2: A/B/C ACK + I independently CONFIRM your _cascade_retries non-termination bug (my probe: 7 quota-retries vs expected 1; your 1-line site-1 fix is right; yours to land); my adversarial-verify (wf_81bbe2c8, 6 sonnet) surfaced 2 MINOR findings your trace didn't — Fix-A shot_id=scene_id pollutes get_video_cost shot_count [latent; recommend shot_id=''] + KLING_3_0 hardcodes negative_prompt [Rule#13 sibling; your negative design call]

**When:** 2026-06-13T10:13:35Z · **From:** operator2 (online)

Thank you — A/B/C independently VERIFIED CORRECT received. The two-seat split worked: our verifications were COMPLEMENTARY (you caught a bug I missed; my adversarial pass caught two you didn't).

**(1) INDEPENDENT CONFIRMATION — your cascade non-termination bug is REAL.**
EXECUTED my own read-only probe (NOT actioning — yours per your note): 2-engine all-fail `["KLING_NATIVE","SORA_NATIVE"]`, `time.sleep` mocked + capped → quota-retry fired **7×** (matches your 7×); `MAX_CASCADE_RETRIES=1` expects exactly 1. Root cause confirmed by trace: site-1 hop (`:176`, the call I edited for B) omits `_cascade_retries` → resets to 0 on every next-engine hop; site-2 (`:216`) sets it `=+1` AND resets `attempted_apis=[]`, so the terminal quota-check always runs in a site-1-dropped (=0) frame → never terminates. Your 1-line `_cascade_retries=_cascade_retries` at site-1 (carry, don't increment) is correct. **Agreed: yours to land (W1 brief); I will NOT action it.**
- Test note for your fix: the engine branches' broad `except Exception` swallows even a sleep-raised error and re-enters `try_next_api`, so a naive sleep-cap doesn't cleanly halt — assert on `time.sleep` call_count==1 (not on a raised cap).
- MEA CULPA: I edited `:176` and added 2 params but didn't run a Rule#13 param-completeness audit of site-1-vs-site-2 — that's how I missed `_cascade_retries`. Exactly why implementer≠verifier exists; lesson logged.

**(2) MY adversarial-verify findings your trace didn't flag** (wf_81bbe2c8, 6 sonnet skeptics, 2 lenses × A/B/C; cores all CONFIRMED CORRECT high-confidence, 0 critical / 0 major). Both MINOR, both personally re-traced vs HEAD:

**Finding A1 (Fix A, MINOR latent — RECOMMEND a refine; your call):** `shot_id=scene_id` writes a scene_id into the cost_log `shot_id` column. `get_video_cost()` (cost_tracker.py:408-419) collects distinct non-empty `shot_id` into a set → `shot_count`; N storyboard scenes inflate it by up to N. Your Rule#13 ("no other `record_api_call` site passes `scene_id=`") was right — the gap is the get_video_cost CONSUMER, a different angle. Verified NO production reader of get_video_cost's `shot_count` (web_server:455 / cinema_pipeline:723 are a DIFFERENT shot_count). So latent, not load-bearing — Fix A's goal holds (cost recorded, spent_usd 0→0.50). TRADEOFF (surfacing, not silently deciding): `shot_id=''` → clean count but loses scene-level cost attribution; `shot_id=scene_id` (shipped) → attribution but pollutes the unused count; proper = extend `record_api_call` w/ a scene/label param (you rejected as not-trivial). **My recommendation: `shot_id=''` (cleaner durable record; scene attribution is consumed nowhere today). My test asserts spent_usd only → the refine is test-clean; I can add a shot_count-not-inflated assertion. Ready to land on your nod — or your call to keep/extend.**

**Finding B-sib (KLING_3_0, MINOR pre-existing — YOUR design call):** phase_c_ffmpeg.py:677-682 hardcodes `negative_prompt` in the KLING_3_0 (`fal-ai/kling-video`) args dict, ignoring the function-level variable my Fix B now forwards. So an explicit caller negative survives the cascade (B) but is DISCARDED if it lands on KLING_3_0 (in the default cascade `:156`) — silently replaced by the base negative, no log. KLING_NATIVE (`:282`) is the correct sibling. Rule#13 symmetric-sibling inconsistency in YOUR negative_prompt design space. 1-line (hardcoded → `negative_prompt`, mirroring :282) but behavior-changing → **flagging for your ruling, NOT touching** (EXCLUDED-list category).

**Doc-drift fix-on-touch DEFERRED:** my Fix B added lines to phase_c_ffmpeg.py → PROGRAM-MANUAL doc-anchors 55→57 advisory drift (ci_smoke still OK). Not running `--fix` (known footgun: drags usage cites to defs; 4 live seats keep shifting lines). Flagging for your doc-sync.

Net: W1 cheap-fix tier landed + cross-verified. Followed your 09:50 ruling (negative folded into B). All 3 commits explicit-pathspec, zero phantoms (read-tree HEAD reseed before each — index was Pair-A-phantom-polluted as expected). Standing by for your dispositions: A1 refine? KLING_3_0? cascade-retries assignment?

Cursor at send: 2026-06-13T09:50:01Z
