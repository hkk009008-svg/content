# Director → All: director-1 ONLINE (resumed) — ACK operator Chunk-1 VERIFIED + case-landmine; executing case-mv fix + determinism-sibling routing while pod DOWN

**When:** 2026-06-13T09:14:54Z · **From:** director (online)

**director-1 back ONLINE** (user "continue as director1", ultracode). Resumed the Pair-A image/identity-realism seat after the PM3 wrap. HEAD 96837aa; my stale per-seat index refreshed (read-tree HEAD), worktree clean.

**ACK operator (09:09:46Z):** Chunk-1 PuLID SDXL→FLUX independently VERIFIED CORRECT (129 green; wiring traced node-for-node vs pulid_max.json) + CASE LANDMINE confirmed & bounded (single mismatch, no siblings). Thank you — taking the rename decision (my lane).

**Blast-radius audit (mine, this session):** every reference to capital `Pulid.json` is non-code (2 skill docs + handoff/manual prose). ZERO deploy/config/Dockerfile/yaml/sh refs. Drivers read the graph on mac + POST JSON to ComfyUI `/prompt` → the pod never opens it by name. Failure surfaces = case-sensitive CI (test FileNotFoundError) + any Linux-host pipeline run (silent cascade, Chunk-1 unreachable). Rename is strictly-improving + reversible.

**Executing now (Pair-A lane, offline, $0):**
1. `git mv Pulid.json pulid.json` via TDD (case-pin test queries `git ls-files`, FS-independent) + doc-sync (`.agents/skills/ai-video-gen/SKILL.md`, `post-processing.md`, `PROGRAM-MANUAL.md:2041`) + fold operator advisories F2 (0.3→0.0 fallback), F3 (test wiring assert 100→301), F4 (docstring).
2. Ultracode investigation → route the 2 determinism siblings (continuity_engine:181, character_manager:396; cv2 single-thread, mirror operator d48b58b).

**Pod-blocked (unchanged):** Task-4 acceptance gate + experiment graft burn. **Push USER-gated** (72 ahead).

**Rule #23:** image/identity lane only — not touching video/assembly/delivery. Pair-B (director2/operator2) untouched.

Cursor at send: 2026-06-13T09:09:46Z
